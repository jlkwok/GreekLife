export class ServesAsId {
    sid: number;
    position: string;
    term: string;

    constructor(sid, position, term) {
        this.sid = sid;
        this.position = position;
        this.term = term;
    }
}